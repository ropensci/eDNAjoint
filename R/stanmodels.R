# Generated by rstantools.  Do not edit by hand.

# names of stan models
stanmodels <- c("joint_binary_catchability_negbin", "joint_binary_catchability_pois", "joint_binary_cov_catchability_negbin", "joint_binary_cov_catchability_pois", "joint_binary_cov_negbin", "joint_binary_cov_pois", "joint_binary_negbin", "joint_binary_pois", "traditional_catchability_negbin", "traditional_catchability_pois", "traditional_negbin", "traditional_pois")

# load each stan module
Rcpp::loadModule("stan_fit4joint_binary_catchability_negbin_mod", what = TRUE)
Rcpp::loadModule("stan_fit4joint_binary_catchability_pois_mod", what = TRUE)
Rcpp::loadModule("stan_fit4joint_binary_cov_catchability_negbin_mod", what = TRUE)
Rcpp::loadModule("stan_fit4joint_binary_cov_catchability_pois_mod", what = TRUE)
Rcpp::loadModule("stan_fit4joint_binary_cov_negbin_mod", what = TRUE)
Rcpp::loadModule("stan_fit4joint_binary_cov_pois_mod", what = TRUE)
Rcpp::loadModule("stan_fit4joint_binary_negbin_mod", what = TRUE)
Rcpp::loadModule("stan_fit4joint_binary_pois_mod", what = TRUE)
Rcpp::loadModule("stan_fit4traditional_catchability_negbin_mod", what = TRUE)
Rcpp::loadModule("stan_fit4traditional_catchability_pois_mod", what = TRUE)
Rcpp::loadModule("stan_fit4traditional_negbin_mod", what = TRUE)
Rcpp::loadModule("stan_fit4traditional_pois_mod", what = TRUE)

# instantiate each stanmodel object
stanmodels <- sapply(stanmodels, function(model_name) {
  # create C++ code for stan model
  stan_file <- if(dir.exists("stan")) "stan" else file.path("inst", "stan")
  stan_file <- file.path(stan_file, paste0(model_name, ".stan"))
  stanfit <- rstan::stanc_builder(stan_file,
                                  allow_undefined = TRUE,
                                  obfuscate_model_name = FALSE)
  stanfit$model_cpp <- list(model_cppname = stanfit$model_name,
                            model_cppcode = stanfit$cppcode)
  # create stanmodel object
  methods::new(Class = "stanmodel",
               model_name = stanfit$model_name,
               model_code = stanfit$model_code,
               model_cpp = stanfit$model_cpp,
               mk_cppmodule = function(x) get(paste0("rstantools_model_", model_name)))
})
